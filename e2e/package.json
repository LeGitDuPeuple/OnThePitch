{
  "name": "onthepitch-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Tests de bout en bout (Playwright, Page Object Model) — vérifie front + back + base ensemble, contrairement aux tests Jest de backend/ qui isolent la couche Service (voir CLAUDE.md).",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.0.0",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "otplib": "^13.5.0"
  }
}
