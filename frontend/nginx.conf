server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Repli sur index.html pour toute route inconnue de nginx : react-router
  # (BrowserRouter) gère la navigation côté client, ex. /evenements/42.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
